from setuptools import setup, find_packages

setup(
        name="retfindings",
        version="1.0.0",
        authors=["Melissa de la Pava", "Juan S. Lara"],
        packages=find_packages(),
        description="Tensorflow implementation of different neural networks to classify eye findings in diabetic retinopathy",
        license="MIT"
)
